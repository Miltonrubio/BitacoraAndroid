package com.example.bitacora;
/*
import androidx.lifecycle.LiveData;
import androidx.lifecycle.ViewModel;
import androidx.paging.LivePagedListBuilder;
import androidx.paging.PagedList;

public class NombreActividadViewModel extends ViewModel {

    private LiveData<PagedList<NombreActividad>> nombreActividadLiveData;

    public NombreActividadViewModel(NombreActividadDataSourceFactory dataSourceFactory) {
        PagedList.Config config = new PagedList.Config.Builder()
                .setPageSize(10) // Cantidad de elementos por página
                .setEnablePlaceholders(false) // Si quieres que se muestren espacios en blanco mientras se cargan los datos
                .build();

        nombreActividadLiveData = new LivePagedListBuilder<>(dataSourceFactory, config).build();
    }

    public LiveData<PagedList<NombreActividad>> getNombreActividadLiveData() {
        return nombreActividadLiveData;
    }
}
*/